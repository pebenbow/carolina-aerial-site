Run `quarto preview` to start the local development server for this project.

```bash
quarto preview
```

This starts a live-reload server at http://localhost:4848 (or the next available port). The browser will open automatically. Press Ctrl+C to stop.
